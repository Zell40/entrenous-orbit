# orbit-bac-live

**Fusionné dans `orbit-petitbac` (v37).**

Le tableau live (grille joueurs × catégories, récap Markdown, `/bacboard`) est désormais dans le plugin unique `orbit-petitbac`. Ce fichier n’est plus chargé par `config.json`.
