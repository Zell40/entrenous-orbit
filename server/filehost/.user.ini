; PHP-FPM reads this next to filehost-upload.php (WEBROOT).
; Without it, default upload_max_filesize=2M silently drops composer uploads
; and the script only sees a generic upload_failed.
upload_max_filesize = 16M
post_max_size = 16M
