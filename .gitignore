# Secrets (never commit)
*.local.php
*.local

# Runtime invite store (www-data writable; created on deploy)
plugins/orbit-conference/visio-invite-data/

# Server-side deploy markers (test + prod)
.last-deployed-commits
.last-deployed-commits-prod

# OS / editor
.DS_Store
.idea
.vscode
*.sw?
Thumbs.db

# Logs
*.log
